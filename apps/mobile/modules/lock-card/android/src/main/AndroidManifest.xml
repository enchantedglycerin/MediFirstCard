<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!--
      Re-posts the pinned card after a swipe-away (our explicit DISMISSED action, sent with
      setClass so no intent-filter is needed for it), after a reboot, and after an app update.
      BOOT_COMPLETED and MY_PACKAGE_REPLACED are protected system broadcasts, so exported="true"
      is safe: only the OS can deliver them.
    -->
    <receiver
      android:name="expo.modules.lockcard.LockCardReceiver"
      android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
