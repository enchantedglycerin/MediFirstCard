# Work log

Dated entries per member, for IPAC evidence. One line per work session.

## Week 0 (Phase 0)

- 2026-09-03 — DEV: monorepo scaffold, `@mfc/shared` package, API health scaffold, CI, docker-compose (AI-assisted, reviewed).
- 2026-09-03 — DEV: W2 backend — DB layer (PGlite/node-postgres + Drizzle schema + migrations), auth (scrypt + JWT rotation), profile + sub-collections + emergency card (QR), records upload/extract(mock) pipeline, field encryption. 27 tests pass; live server smoke ran the full flow (AI-assisted, reviewed).
- 2026-09-03 — DEV: W4 backend slice — public rescuer page (/e/:token + .json + qr.png), clinician share links (/s/:token + passcode + revoke + access log), card-viewed alert workflow (in-app notifications; email stub), consent + right-to-erasure (DELETE /me). 31 tests total (11 shared + 20 api); 36 API routes; esbuild bundle builds (AI-assisted, reviewed).
- 2026-09-03 — DEV: mobile app scaffolded (Expo SDK 57 + Expo Router + React Native Paper + Sarabun). Theme tokens, i18n (th/en from @mfc/shared), session store + SecureStore, typed API client with refresh-on-401, local PIN. Screens: login/register/consent, tabs (home/card/records/more), profile editor, lock-screen field picker, records scan→extract→review, rescuer view. Whole app typechecks and bundles with Metro (expo export, 2000+ modules) (AI-assisted, reviewed).
- 2026-09-03 — DEV: **ran on a physical device.** Debug APK built (BUILD SUCCESSFUL, 1h4m first native build) and installed on the connected phone (R58N21293HM). Drove register → consent → home over adb: registration created a user in the DB via the phone; the home dashboard shows live API counts. Screenshots in docs/screenshots/ (01-login, 02-consent, 03-home). Full stack verified on-device (AI-assisted, reviewed).
- 2026-09-04 — DEV: **mobile app rebuilt end to end** after on-device review found thin, half-working screens. Contract layer first (typed API client for every endpoint incl. contacts CRUD, share links, notifications, consent/erasure; i18n with ~400 keys in TH+EN; card lines carry a dialable `phone`; Thai EMS 1669), then every screen rewritten: login/register/consent, PIN + biometric lock, dashboard with profile-completeness checklist, full emergency-profile editor, allergies/conditions/medications/**emergency contacts** editors, emergency card with tap-to-call + Call 1669 + QR + share, rescuer preview, lock-screen card as a pinned notification, scan (camera/gallery) → editable red-field review → record detail/delete, clinician share links, alerts, privacy (withdraw consent / delete account), settings. ~4,400 lines (was 1,100). tsc clean; shared 13 + API 21 tests green. Verified on the phone: register → consent → profile → contact → card → dialer opens with the number → pinned notification on the system shade → gallery scan → mock extraction → red-field review → saved record. Screenshots in docs/screenshots/ (AI-assisted, reviewed).
- 2026-09-04 — TEST: on-device pass 2 (same phone): clinician link created in the app → public page asks for the passcode, rejects a wrong one (401), shows the record with the right one; alerts list shows "card viewed" and "link opened" from those visits; PIN set in More → cold restart shows the PIN gate → unlock; Thai/English switch persists, Buddhist-era dates. Found and fixed on the way: concurrent token refresh signing the user out, expo-crypto digest needing a typed array, Snackbars outside Portal, tab bar under the nav buttons. 18 screenshots in docs/screenshots/ (AI-assisted, reviewed).
- 2026-09-04 — DEV: live provider adapters — Gemini structured extraction (REST `generateContent` + `responseJsonSchema`), Typhoon OCR (OpenAI-compatible `/chat/completions`, exact v1.5 prompt), orchestrator with retries and a labelled mock fallback, Supabase Storage REST adapter; `EXTRACT_PROVIDER=gemini` and `STORAGE_PROVIDER=supabase` no longer return 501. Native `fetch` only, injectable for offline tests. tsc clean; shared 13 + API 63 tests green; `.env.example`, README and decisions updated (AI-assisted, reviewed).
- 2026-09-04 — DEV: release APK built (cleartext enabled for the LAN demo, signed with the debug keystore), cold-started standalone on the phone with the session intact; published as GitHub pre-release `demo-2026-09-04`. Synthetic sample documents and the demo runbook added (AI-assisted, reviewed).
